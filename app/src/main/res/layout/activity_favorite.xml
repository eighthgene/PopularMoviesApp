<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.FavoriteActivity">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv_favorite"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical">

    </android.support.v7.widget.RecyclerView>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/iv_favorite_empty"
            android:layout_width="@dimen/iv_width_favorite_empty_image_view"
            android:layout_height="@dimen/iv_height_favorite_empty_image_view"
            android:layout_gravity="center"
            android:src="@drawable/ic_favorite_empty"
            android:visibility="invisible"
            android:contentDescription="@string/msg_favorite_empty" />

        <TextView
            android:id="@+id/tv_favorite_empty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:lines="2"
            android:paddingTop="16dp"
            android:text="@string/msg_favorite_empty"
            android:textAlignment="center"
            android:textSize="@dimen/msg_no_connection"
            android:visibility="invisible" />
    </LinearLayout>

</FrameLayout>